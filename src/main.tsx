import React from "react";
import ReactDOM from "react-dom/client";
import VideoCalling from "./App"; // default export

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <VideoCalling />
  </React.StrictMode>
);
